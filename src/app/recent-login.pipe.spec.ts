import { RecentLoginPipe } from './recent-login.pipe';

describe('RecentLoginPipe', () => {
  it('create an instance', () => {
    const pipe = new RecentLoginPipe();
    expect(pipe).toBeTruthy();
  });
});
